import React from 'react'
import ReactDOM from 'react-dom/client'
import Pagina from './paginas/PaginadeHome'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Pagina />
  </React.StrictMode>
)
